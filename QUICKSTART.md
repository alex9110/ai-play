# Быстрый старт / Quick Start

## 🐳 Способ 1: С Docker (Рекомендуется)

### Шаг 1: Запустите Docker Desktop
- Откройте приложение **Docker Desktop** на вашем Mac
- Дождитесь, пока Docker полностью запустится (иконка в меню должна быть зеленая)

### Шаг 2: Запустите проект
```bash
cd /Users/oleksandr/Desktop/projects/experements
docker-compose up --build
```

### Шаг 3: Откройте в браузере
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **API Документация**: http://localhost:8000/docs

---

## 💻 Способ 2: Без Docker (Локально)

### Backend

1. **Откройте терминал и перейдите в папку backend**:
```bash
cd /Users/oleksandr/Desktop/projects/experements/backend
```

2. **Создайте виртуальное окружение**:
```bash
python3 -m venv venv
source venv/bin/activate
```

3. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

4. **Запустите сервер**:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен на http://localhost:8000

### Frontend

1. **Откройте новый терминал и перейдите в папку frontend**:
```bash
cd /Users/oleksandr/Desktop/projects/experements/frontend
```

2. **Установите зависимости**:
```bash
npm install
```

3. **Запустите dev сервер**:
```bash
npm run dev
```

Frontend автоматически откроется на http://localhost:3000

---

## 📝 Первые шаги после запуска

1. **Сгенерируйте датасет**:
   - В веб-интерфейсе нажмите "Generate Dataset"
   - Или через API:
   ```bash
   curl -X POST http://localhost:8000/dataset/generate \
     -H "Content-Type: application/json" \
     -d '{"num_samples": 10000}'
   ```

2. **Обучите модель**:
   - В веб-интерфейсе настройте параметры и нажмите "Start Training"
   - Или через API:
   ```bash
   curl -X POST http://localhost:8000/train/start \
     -H "Content-Type: application/json" \
     -d '{"epochs": 50, "batch_size": 64}'
   ```

3. **Сделайте предсказание**:
   - Введите число в поле и нажмите "Factorize"
   - Или через API:
   ```bash
   curl -X POST http://localhost:8000/predict \
     -H "Content-Type: application/json" \
     -d '{"n": 2021}'
   ```

---

## ❗ Решение проблем

### Docker не запускается
- Убедитесь, что Docker Desktop запущен
- Проверьте: `docker ps` должен работать без ошибок

### Порт занят
- Измените порты в `docker-compose.yml` или остановите другие сервисы

### Ошибки импорта в Python
- Убедитесь, что вы в папке `backend` и виртуальное окружение активировано

### Ошибки npm
- Убедитесь, что Node.js версии 20+ установлен: `node --version`
- Удалите `node_modules` и запустите `npm install` снова

